const greet = (name) =>{
    if (name != undefined)
        console.log(`Hello, My name is ${name}`);
    else
        console.log("invalid name is provided");
};

greet('Vikash');

// just adding the Comment